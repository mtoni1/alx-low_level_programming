#include <stdio.h>

/**
*main - Program that prints single numbers of base 10 using putchar twice
*Return: 0 (success)
*/

int main(void)
{
	int n;
	n = 0;
	while
		(n < 0) {
			putchar(n + '0');
			n++;
		}
	putchar('\n');
	return (0);
}
