#include "main.h"
/**
 * mul - checks for a digit (0 through 'n')
 * @a: a variab;e
 * @b: b variable
 * Return: 0 (success)
 */
int mul(int a, int b)
{
        int mul;

        mul = a*b;
        return(mul);
}
